# 鼠小侠 v3.0 - 键鼠录制回放工具 软件说明书

---

## 一、软件概述

- **软件名称**：鼠小侠 - 曦微出品自动点击器
- **软件版本**：v3.0
- **运行环境**：Windows 10/11
- **软件大小**：约 15MB（单文件）
- **主要功能**：录制鼠标和键盘操作，支持精确回放

---

## 二、界面布局

```
┌─────────────────────────────────────────────┐
│  鼠小侠-曦微出品自动点击器                    │
├─────────────────────────────────────────────┤
│  开始录制(F7)  停止录制(F8)  保存脚本(F9)     │
│  开始回放(F10) 暂停回放(F11) 停止回放(F12)    │
├─────────────────────────────────────────────┤
│  ▶ 状态显示                                  │
│    状态：待机中/录制中/回放中/已暂停           │
│    录制时长：00:00:00    操作步数：0          │
│    回放进度：0/0         回放耗时：00:00:00   │
├─────────────────────────────────────────────┤
│  ▶ 回放设置                                  │
│    循环模式：◉ 单次  ○ 无限循环  ○ 次数 [1]   │
├─────────────────────────────────────────────┤
│  ▶ 脚本管理                                  │
│    脚本列表：[下拉选择]  重命名  删除         │
├─────────────────────────────────────────────┤
│  ▶ 运行日志                                  │
│    [时间戳] 日志内容...                      │
└─────────────────────────────────────────────┘
```

---

## 三、快捷键说明

| 快捷键 | 功能 | 触发条件 |
|--------|------|----------|
| **F7** | 开始录制 | 待机状态下 |
| **F8** | 停止录制 | 录制状态下 |
| **F9** | 保存脚本 | 待机状态下 |
| **F10** | 开始回放 | 待机状态下，且脚本不为空 |
| **F11** | 暂停/恢复回放 | 回放状态下（循环切换） |
| **F12** | 停止回放 | 回放/暂停状态下 |

> **注意**：快捷键在后台全局生效，无需激活软件窗口。

---

## 四、使用流程

### 4.1 录制操作

1. 按下 **F7** 或点击"开始录制"按钮开始录制
2. 执行需要录制的鼠标和键盘操作
3. 按下 **F8** 或点击"停止录制"按钮结束录制
4. 录制内容会实时显示在状态区域（操作步数）

### 4.2 保存脚本

1. 确保处于待机状态
2. 按下 **F9** 或点击"保存脚本"按钮
3. 在弹出的对话框中输入文件名（自动添加 `.mrs` 后缀）
4. 脚本会保存到 `scripts/` 目录（加密格式）

### 4.3 加载脚本

1. 点击"脚本列表"下拉菜单
2. 选择要加载的脚本文件
3. 脚本自动加载到当前操作队列

### 4.4 回放操作

1. 加载脚本或确保当前有录制内容
2. 设置循环模式（单次/无限循环/次数）
3. 按下 **F10** 或点击"开始回放"按钮
4. 回放过程中可按 **F11** 暂停/恢复
5. 按下 **F12** 或点击"停止回放"按钮结束

---

## 五、循环模式说明

| 模式 | 说明 | 输入框状态 |
|------|------|------------|
| **单次** | 回放一次后自动停止 | 禁用 |
| **无限循环** | 持续循环回放，直到手动停止 | 禁用 |
| **次数** | 按指定次数循环回放 | 启用，可输入正整数 |

> **默认模式**：单次

---

## 六、支持录制的动作类型

| 动作类型 | 说明 |
|----------|------|
| 鼠标移动 | 记录鼠标坐标变化 |
| 左键单击 | 记录左键点击 |
| 左键双击 | 记录左键双击 |
| 右键单击 | 记录右键点击 |
| 左键拖动 | 记录左键按住拖动 |
| 右键拖动 | 记录右键按住拖动 |
| 鼠标滚轮 | 记录滚轮滚动方向和距离（精确匹配） |
| 键盘按键 | 记录键盘按下和释放事件 |

> **注意**：快捷键（F7-F12、ESC）不会被记录到脚本中。

---

## 七、脚本管理

1. **脚本列表**：显示 `scripts/` 目录下所有 `.mrs` 脚本文件，按修改时间排序
2. **重命名**：选中脚本后点击"重命名"按钮修改文件名
3. **删除**：选中脚本后点击"删除"按钮删除（需确认）

---

## 八、技术特点

- **高精度时间控制**：采用 `time.perf_counter()` 实现微秒级时间精度
- **滚轮精确匹配**：录制和回放使用相同的 pynput API，确保滚动距离 1:1 一致
- **后台快捷键**：全局热键在后台生效，无需激活窗口
- **脚本加密**：使用 Base64 + XOR 加密存储，防止数据泄露
- **居中显示**：启动时自动居中到屏幕中央

---

## 九、注意事项

1. 录制和回放时，请确保目标应用窗口在可见位置
2. 回放速度固定为 1 倍速，与录制时长一致
3. 关闭软件时会弹出确认对话框，防止误操作
4. 暂停状态下关闭软件会先恢复再停止回放
5. 脚本文件默认保存在程序目录下的 `scripts/` 文件夹

---

## 十、常见问题

**Q1：录制的动作回放时位置不对？**
A：录制和回放时请确保屏幕分辨率一致，窗口位置相同。

**Q2：快捷键不生效？**
A：请检查是否有其他软件占用了 F7-F12 快捷键。

**Q3：脚本保存后找不到？**
A：脚本保存在 `scripts/` 目录下，重启软件后会自动加载到列表。

**Q4：回放时速度与录制不一致？**
A：软件已优化时间精度，理论上应完全一致。如仍有差异，请检查是否有其他程序占用系统资源。

---

## 十一、更新记录

| 版本 | 更新内容 |
|------|----------|
| v3.0 | 优化时间精度、修复暂停/恢复逻辑、删除速度选项、默认单次循环 |
| v2.x | 添加脚本管理、循环模式、快捷键、暂停/恢复功能 |
| v1.x | 基础录制回放功能 |

---

**开发团队**：曦微出品
**联系方式**：无
