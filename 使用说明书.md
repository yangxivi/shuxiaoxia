# 鼠小侠 - 轻量化鼠标录制回放工具 使用说明书

## 一、软件简介

**鼠小侠** 是一款轻量化、无广告的Windows桌面端鼠标操作录制与回放工具。支持全局后台录制、多档位速度回放、循环回放、脚本加密保存等功能，适用于自动化测试、重复操作演示等场景。

- **版本**：v2.0
- **开发语言**：Python 3.10+
- **运行平台**：Windows 10 / Windows 11
- **GUI框架**：tkinter（内置轻量化）

---

## 二、功能特性

### 1. 录制模块
- ✅ 捕获全部鼠标行为：移动轨迹、左键单击/双击、右键单击、滚轮滑动
- ✅ 精准记录：屏幕绝对坐标、操作类型、时间间隔
- ✅ 防误触：点击软件按钮不会误录入
- ✅ 实时显示：录制时长、已录制步数

### 2. 回放模块
- ✅ 五档速度：0.3x / 0.5x / 1x / 2x / 3x
- ✅ 循环模式：单次 / 无限循环 / 指定次数
- ✅ 紧急中断：ESC键立即停止
- ✅ 进度显示：当前步数/总步数、回放耗时

### 3. 脚本管理
- ✅ 加密保存：JSON脚本加密存储，防止乱码
- ✅ 脚本加载：读取历史录制脚本
- ✅ 一键清空：清除当前录制缓存

### 4. 全局快捷键
| 快捷键 | 功能 |
|--------|------|
| F9 | 启动录制 |
| F10 | 停止录制 |
| F11 | 启动回放 |
| ESC | 紧急停止录制/回放 |
| 空格 | 标记关键帧 |

---

## 三、快速开始

### 方式一：直接运行exe（推荐）
1. 双击 `dist\鼠小侠.exe` 启动程序
2. 无需安装Python环境

### 方式二：源码运行
```bash
# 1. 安装依赖
pip install pyautogui pynput

# 2. 运行程序
python mouse_recorder_pro.py
```

---

## 四、操作指南

### 录制操作
1. 点击 **【开始录制(F9)】** 或按 `F9`
2. 等待3秒倒计时（防误触）
3. 进行需要录制的鼠标操作
4. 点击 **【停止录制(F10)】** 或按 `F10` 结束录制

### 回放操作
1. 在 **回放设置** 区选择速度和循环模式
2. 点击 **【开始回放(F11)】** 或按 `F11`
3. 按 `ESC` 可随时中断回放

### 脚本管理
- **保存脚本**：点击保存按钮，选择保存位置，文件格式为 `.mrs`
- **加载脚本**：点击加载按钮，选择已保存的脚本文件
- **清空记录**：清除当前录制的所有动作

---

## 五、界面说明

```
┌─────────────────────────────────────────┐
│  状态显示区                              │
│  ● 待机中 / 录制中 / 回放中              │
│  录制时长: 00:00:00  操作步数: 0         │
│  回放进度: 0/0       回放耗时: 00:00:00  │
├─────────────────────────────────────────┤
│  功能操作区                              │
│  [开始录制(F9)] [停止录制(F10)] [清空]   │
│  [开始回放(F11)] [停止回放(ESC)] [关键帧]│
├─────────────────────────────────────────┤
│  回放设置区                              │
│  回放速度: ○0.3x ○0.5x ●1x ○2x ○3x     │
│  循环模式: ○单次 ○无限循环  指定次数:[1] │
├─────────────────────────────────────────┤
│  脚本管理区                              │
│  [保存脚本] [加载脚本] 当前脚本: 无      │
├─────────────────────────────────────────┤
│  运行日志                                │
│  [HH:MM:SS] 程序已启动...               │
└─────────────────────────────────────────┘
```

### 状态指示灯颜色
- 🔴 **红色**：录制中
- 🟢 **绿色**：回放中
- ⚪ **灰色**：待机中

---

## 六、注意事项

1. **权限要求**：程序需要管理员权限才能监听全局鼠标事件
2. **防误触**：录制开始后有3秒延时和500ms忽略期
3. **紧急停止**：
   - 按 `ESC` 键
   - 或将鼠标快速移到屏幕左上角（FAILSAFE机制）
4. **脚本格式**：保存的 `.mrs` 文件为加密格式，不可直接编辑

---

## 七、常见问题

### Q1: 快捷键不生效？
- 确保程序以管理员权限运行
- 检查是否有其他软件占用了相同快捷键

### Q2: 录制坐标偏移？
- 检查系统显示缩放比例是否为100%
- 高DPI屏幕可能需要调整缩放设置

### Q3: 回放时鼠标失控？
- 按 `ESC` 紧急停止
- 或快速将鼠标移到屏幕左上角

### Q4: 脚本加载失败？
- 确认文件格式为 `.mrs`
- 脚本可能已损坏，请重新录制

---

## 八、技术架构

```
┌─────────────────────────────────────────────┐
│              UI界面层 (tkinter)              │
│  MouseRecorderApp - 主窗口、按钮、日志显示   │
├─────────────────────────────────────────────┤
│            监听层 (pynput.mouse)            │
│  MouseEventListener - 全局鼠标事件监听       │
├─────────────────────────────────────────────┤
│           回放模拟层 (pyautogui)            │
│  ActionPlayer - 鼠标动作模拟回放             │
├─────────────────────────────────────────────┤
│          数据存储层 (json + 加密)           │
│  ScriptStorage - 脚本加密保存/加载           │
└─────────────────────────────────────────────┘
```

---

## 九、打包说明

### 使用一键打包脚本
双击 `build.bat` 即可自动完成打包。

### 手动打包命令
```bash
# 安装依赖
pip install pyautogui pynput pyinstaller

# 打包为单文件exe
pyinstaller --noconfirm --onefile --windowed --name "鼠小侠" --collect-submodules pynput --collect-submodules pyautogui mouse_recorder_pro.py
```

### 参数说明
| 参数 | 说明 |
|------|------|
| `--onefile` | 打包为单个exe文件 |
| `--windowed` | 无控制台窗口（GUI程序） |
| `--name` | 输出文件名 |
| `--collect-submodules` | 收集指定库的所有子模块 |

---

## 十、更新日志

### v2.0 (2024)
- 全新架构设计，四层分层解耦
- 新增关键帧标记功能
- 新增五档速度调节
- 新增无限循环回放
- 脚本加密存储
- 状态指示灯
- 完善异常处理

---

## 十一、联系方式

如有问题或建议，欢迎反馈。

**作者**：资深桌面端Python开发工程师
**版本**：v2.0
